# todo
per-agent task prompt — editable string injected into system prompt every turn so the model never loses track of the task
chat mode should alter the system prompt — tell the model it's in chat mode, no commands, just conversation
consider switching to tool use API or JSON for command parsing instead of <func> XML tags

multi agent? still maintain individual dockers. maybe they communicate through github? maybe i set up a agent account for them in docker, just one account  for all of them. maybe build a in program chat room for them. <chat>[group][message] that gets added as role chat to all agents in that group.  

make youtube videos by functionality like theres a intvrface series and each video is a new functionality like first video will cover all the basics i have done now and in this way future videos won't conflict with present ones and there's a serial relationship
